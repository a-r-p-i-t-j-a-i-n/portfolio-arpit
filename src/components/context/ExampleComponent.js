// import React, { useContext } from 'react';
// import MyContext from './MyContextProvider'; // Import the context

const ExampleComponent = () => {
  // Access context data and function using useContext hook
  // const { someData, updateData } = useContext(MyContext);

  return (
    <div>
      {/* <h1>Example Component</h1>
      <p>Data from Context: {someData}</p>
      <button onClick={() => updateData('New Value')}>
        Update Context Data
      </button> */}
    </div>
  );
};

export default ExampleComponent;
